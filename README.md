# EMP-Management-System
A Java-based console application that manages employee records using OOP principles and CRUD operations (add, view, update, delete). It uses collections like HashMap for efficient data handling, along with input validation and exception handling to ensure reliable and structured record management.
